# Create a fucntion that will handle simple math expressions. The input is an expression in the form of a string.


def calculator(txt):
    return eval(txt) # using the [eval()] function, we can parse the given "txt" or expression and runs the python expression in the program. Allowing us to convert a string value to an int value